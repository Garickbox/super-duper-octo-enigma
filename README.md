# Telegram Camera Bot

Персональное веб-приложение для отправки фото в Telegram с подробным логированием.

## 👤 Пользователь
- **Имя:** Александр Горчаков
- **User ID:** 1189539923

## 📊 Система логирования

### Уровни логирования:
- **INFO:** Основные события системы
- **ERROR:** Ошибки и исключения
- **WARN:** Предупреждения
- **DEBUG:** Отладочная информация

### Файлы логов:
- Логи сохраняются в папке `logs/`
- Формат: `server-YYYY-MM-DD.log`
- Каждая запись в формате JSON

### API для мониторинга:

| Endpoint | Описание |
|----------|----------|
| `GET /api/logs` | Последние 100 записей логов |
| `GET /api/stats` | Статистика системы |
| `GET /api/bot-status` | Статус Telegram бота |
| `GET /api/test` | Тест работоспособности |

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск сервера
npm start

# Режим разработки с авто-перезагрузкой
npm run dev

# Просмотр логов в реальном времени
npm run logs

# Проверка статуса
npm run status
